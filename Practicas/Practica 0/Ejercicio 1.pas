{Implemente un programa que lea por teclado dos números enteros e imprima en pantalla los valores leídos en orden inverso. Por ejemplo, 
si se ingresan los números 4 y 8, debe mostrar el mensaje:  Se ingresaron los valores 8 y 4}

program Ejer1;
var 
 numero:integer;
 numero2:integer;
begin
	read(numero);
	read(numero2);
	writeln(numero2);
	writeln(numero);
end.
